# 참여자 화면 이탈 5분 자동 로그아웃

GitHub 저장소 `hongisbest/hongisbest-ver.2` 최상단의 기존 `index.html`을 이 파일로 교체합니다.

동작:
- 참여자 화면을 보고 있는 동안에는 로그인 유지
- 다른 앱, 다른 탭, 화면 잠금 등으로 화면을 벗어나면 시간 측정 시작
- 5분 이내 복귀 시 로그인 유지
- 5분 이후 복귀하거나 사이트를 다시 열면 자동 로그아웃
- 자동 로그아웃 후 사번 입력 화면에 안내 표시
- 직접 로그아웃하면 즉시 로그인 정보 삭제
- 관리자 `admin.html`에는 적용되지 않음
- Supabase SQL 변경 없음

